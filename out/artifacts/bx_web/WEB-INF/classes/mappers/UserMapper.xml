<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.qst.bx_web.dao.UserDao">
    <resultMap id="userList" type="com.qst.bx_web.pojo.User">
        <id column="id" property="id"></id>
        <id column="email" property="email"></id>
        <id column="password" property="password"></id>
        <id column="username" property="username"></id>
        <id column="role" property="role"></id>
        <id column="status" property="status"></id>
        <id column="regTime" property="regTime"></id>
        <id column="regIp" property="regIp"></id>
    </resultMap>
    <select id="getAllUser" resultMap="userList">
        SELECT *
        FROM user
    </select>

    <select id="userLogin" resultType="com.qst.bx_web.pojo.User">
        SELECT *
        FROM user
        where username = #{username}
          and `password` = #{password}
    </select>

    <insert id="addUser" parameterType="com.qst.bx_web.pojo.User" useGeneratedKeys="true" keyProperty="id">
        insert into user(email, `password`, username, `role`, `status`, regTime, regIp)
        values (#{email}, #{password}, #{username}, #{role}, #{status}, #{regTime}, #{regIp})
    </insert>
</mapper>

